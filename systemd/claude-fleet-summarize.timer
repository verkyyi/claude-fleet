# Fires claude-fleet-summarize.service. Parity with the launchd plist:
# StartInterval=180 + RunAtLoad=true.
[Unit]
Description=claude-fleet summarizer timer (every 180s)

[Timer]
OnStartupSec=20
OnUnitActiveSec=180
AccuracySec=10s

[Install]
WantedBy=timers.target
