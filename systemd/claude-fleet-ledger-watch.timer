# Fires claude-fleet-ledger-watch.service. Parity with the launchd plist:
# StartInterval=60 + RunAtLoad=true.
[Unit]
Description=claude-fleet ledger-watch daemon timer (every 60s)

[Timer]
OnStartupSec=30
OnUnitActiveSec=60
AccuracySec=10s

[Install]
WantedBy=timers.target
