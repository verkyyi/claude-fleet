# Fires claude-fleet-cleanup.service. Parity with the launchd plist:
# StartInterval=60 + RunAtLoad=true.
[Unit]
Description=claude-fleet cleanup daemon timer (every 60s)

[Timer]
OnStartupSec=25
OnUnitActiveSec=60
AccuracySec=10s

[Install]
WantedBy=timers.target
